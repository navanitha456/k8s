---
apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: replicaset
  labels:
    dev: cloud
spec:
  minReadySeconds: 2
  replicas: 10
  selector:
    matchLabels:
      dev: cloud
  template:
    metadata:
      name: labels
      labels:
        dev: cloud
    spec:
      containers:
        - name: replicas
          image: jenkins/jenkins
          ports:
            - containerPort: 8080